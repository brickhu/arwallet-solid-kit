import { PermissionType } from '../wallet';
import { default as Strategy } from './Strategy';

export declare const STRATEGY_STORE = "wallet_kit_strategy_id";
/**
 * On page mount, sync the logged in strategy
 *
 * @param strategies List of strategies used by the library
 * @param requiredPermissions The permissions required by this app
 * @param enforcePermissions Should the strategy be active only if the required permissions are gived or not
 */
export declare function syncStrategies(strategies: Strategy[], requiredPermissions: PermissionType[], enforcePermissions: boolean): Promise<false | Strategy | undefined>;
/**
 * Save active strategy to localstorage
 */
export declare function saveStrategy(active: string): void;
/**
 * Get strategy by id
 */
export declare function getStrategy(id: string | false, strategies: Strategy[]): Strategy | undefined;
