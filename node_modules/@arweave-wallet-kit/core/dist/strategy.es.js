import { c } from "./permissions-56f2904e.js";
class g {
}
const s = "wallet_kit_strategy_id";
async function S(t, n, r) {
  let i = (localStorage == null ? void 0 : localStorage.getItem(s)) || !1;
  if (i && a(i, t))
    return a(i, t);
  for (const e of t) {
    const o = await e.getPermissions();
    if (!r && o.length > 0)
      return l(e.id), a(e.id, t);
    if (r && c(n, o))
      return l(e.id), a(e.id, t);
  }
  return !1;
}
function l(t) {
  localStorage && localStorage.setItem(s, t);
}
function a(t, n) {
  return n.find((r) => r.id === t);
}
export {
  s as STRATEGY_STORE,
  g as Strategy,
  a as getStrategy,
  l as saveStrategy,
  S as syncStrategies
};
