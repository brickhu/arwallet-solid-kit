import { default as BrowserWalletStrategy } from '@arweave-wallet-kit/browser-wallet-strategy';
import { Strategy } from '@arweave-wallet-kit/core/strategy';
import { DataItem } from 'arconnect';

export default class WanderStrategy extends BrowserWalletStrategy implements Strategy {
    id: string;
    name: string;
    description: string;
    theme: string;
    logo: string;
    url: string;
    constructor();
    isAvailable(): Promise<boolean>;
    addToken(id: string): Promise<void>;
    batchSignDataItem(dataItems: DataItem[]): Promise<ArrayBufferLike[]>;
}
